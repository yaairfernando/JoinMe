$(".comments-popup").toggleClass("popup-comments-hide");

if ($(".comments-popup").length) {
  setTimeout(() => {
    document.querySelector('.comments-popup__container').classList.add('popup-display')
  }, 5)
}
$(".comments-popup").html("<%= j render partial: 'comment' %>")